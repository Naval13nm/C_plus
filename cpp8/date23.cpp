#include <iostream>
#include <vector>
#include <set>
using namespace std;

int main()
{
    //taking value from vector and printing its half value in set from st

    // int n=6;
    // vector<int> v = {};

    // for (int i = 0; i <n; i++)
    // {
    //     int value;
    //     cin >> value;
    //     v.push_back(value);
    // }

    // set<int> s;

    // for (int i = 0; i < v.size() / 2; i++)
    // {
    //     s.insert(v[i]);
    // }

    // for (int i = 0; i <x.size(); i++)    not work here cause in set indexing don't work
    // {
    //     cout<<x[i];
    // }

    // for (auto x : s)
    // {
    //     cout << x;
    // }

    //taking value from vector and printing its half value in set from reverse

    //  int n=6;
    // vector<int> v = {};

    // for (int i = 0; i <n; i++)
    // {
    //     int value;
    //     cin >> value;
    //     v.push_back(value);
    // }

    // set<int> s;

    // for (int i = v.size()-1; i >=v.size() / 2; i--)
    // {
    //     s.insert(v[i]);
    // }

    // for (auto x : s)
    // {
    //     cout << x;
    // }

    

    //take input if + cll reverse fx and reverse vector else cll sort fx

    
    int n=2;
    vector<int>v={1,2,3,4,5}
    if (n>=0)
    {
        Reverse(v);
    }
    
    return 0;
}